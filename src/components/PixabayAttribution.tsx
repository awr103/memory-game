import React from "react"; import { Link, Typography } from "@mui/material"; import { ImageCredit } from "../types"; interface PixabayAttributionProps {credit: ImageCredit;} const PixabayAttribution: React.FC<PixabayAttributionProps> = ({ credit }) => {return (<Typography variant="caption" display="block">Image by <Link href={credit.pageUrl} target="_blank" rel="noopener noreferrer">{credit.user}</Link> on Pixabay</Typography>);}; export default PixabayAttribution;
